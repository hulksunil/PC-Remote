name: pc_remote_client
description: A project to control your PC from your phone.

publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 0.0.1+1

environment:
  sdk: ^3.6.0

dependencies:
  flutter:
    sdk: flutter

  english_words: ^4.0.0
  provider: ^6.1.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  network_info_plus: ^5.0.3

  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true